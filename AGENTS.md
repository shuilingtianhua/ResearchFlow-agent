# ResearchFlow Agent 开发约定

本文件对整个仓库生效。自动化开发 Agent 在分析、修改、测试和发布代码时必须遵守这些约定。

## 1. 项目目标

ResearchFlow Agent 是基于 Sea-mult-agent 业务语义、使用 Python 重构的多智能体科研执行系统。系统需要把研究目标转化为可执行计划，在受控环境中执行任务和实验，并保留可恢复、可审计的状态、事件、证据与产物。

设计输入的优先级如下：

1. `docs/PRD.md`：产品范围、业务术语和验收标准。
2. `docs/ARCHITECTURE.md`：模块边界、依赖方向和技术决策。
3. 当前仓库的接口、测试和既有代码风格。
4. Sea-mult-agent：计划图、状态机、租约、预算、事件和 AutoResearch 可信边界。
5. AGI-saber：仅参考 Python 工程组织、适配器和测试习惯。

不要逐文件翻译 Sea-mult-agent，也不要复制 Saber 的巨型 `UnifiedAgent` 结构。未经用户明确要求，不得修改同级目录中的参考项目。

## 2. 当前阶段

当前已经完成 M1 可运行闭环：

```text
研究目标 -> 固定 DAG -> Fake Capability -> 状态迁移 -> 输出与事件 -> HTTP 查询
```

当前限制：

- RunStore 使用内存实现，进程重启后数据丢失。
- Planner 和 Capability 是确定性测试实现，尚未连接真实模型。
- 事件接口返回历史 JSON，尚未实现 SSE 实时推送。
- Docker Sandbox、Git Workspace、SQLite/PostgreSQL 和真实 Artifact Store 尚未接入。

新增功能应按可运行的垂直切片推进，不要先批量创建没有行为的空目录或抽象层。

## 3. 目录和职责

```text
src/researchflow/
├── domain/          # 无框架依赖的领域模型、状态和错误
├── runtime/         # 命令、调度、状态迁移、执行隔离和存储端口
├── planning/        # 从研究上下文构建并校验计划图
├── capabilities/    # 原子科研能力的窄接口和注册表
├── autoresearch/    # 冻结研究契约、候选、试验和 Keep/Reject 语义
├── artifacts/       # 内容寻址的产物存储端口
├── adapters/        # 数据库、模型、Docker、Git、文件系统等外部实现
├── api/             # FastAPI 请求、响应和错误映射
├── bootstrap.py     # 唯一生产组合根
└── settings.py      # 显式环境配置
```

依赖必须指向领域和消费方定义的端口：

- `domain` 不得依赖 FastAPI、数据库、模型 SDK 或具体适配器。
- `api` 只做校验、路由和协议转换，不实现状态规则或任务编排。
- `runtime` 拥有 Run 生命周期；适配器不能绕过它直接修改状态。
- 外部 IO 必须收敛到 `adapters`，异常在边界转换为项目错误语义。
- `bootstrap.py` 负责组装真实实现，业务模块内部不得偷偷创建全局依赖。
- 不创建无边界的 `utils.py`、`common.py` 或同时拥有全部依赖的万能 Agent 类。

只有存在真实替换需求或测试替身时才增加 Protocol/端口。接口应放在消费方一侧并保持窄小。

## 4. Python 规范

- 支持 Python 3.11 及以上版本。
- 公共接口必须有完整类型标注；领域对象优先使用不可变 dataclass。
- Pydantic 用于 HTTP、配置和外部文件等边界，不用它承载所有领域行为。
- 异步 IO 必须正确传播取消和超时；并发优先使用 AnyIO 结构化并发。
- 捕获具体异常。能力插件边界确需捕获 `Exception` 时，必须转换为失败状态和事件，不能静默返回成功。
- 注释说明业务原因、可信边界和非显然选择，不复述代码步骤。
- 单个函数原则上不超过 50 行，避免三层以上嵌套。
- 不引入没有当前调用方的提前抽象，也不为了“框架完整”增加空实现。

新增依赖前先确认标准库和现有依赖无法满足需求，并同步更新 `pyproject.toml` 和相关构建验证。

## 5. 状态与可信边界

以下规则属于核心业务契约，修改时必须补测试：

- 任务只有在全部依赖成功后才能进入 `READY`。
- 状态快照和事件必须通过 `RunStore.commit()` 原子提交。
- 每次任务执行必须获得新的 `execution_id`；暂停、取消或重试后的迟到结果不得覆盖新状态。
- 事件序号在单个 Run 内严格单调递增。
- 暂停会使当前执行失效；恢复后使用新的执行 ID 重新执行。
- 失败、重试、取消和被忽略的迟到结果都必须留下事件证据。
- 重试不得突破任务策略和 Run 总预算。
- 产物必须记录摘要、schema version、生产任务和存储位置。

AutoResearch 还必须遵守：

- 模型只提出候选，确定性执行器和评测器决定 Keep/Reject。
- 仓库版本、数据摘要、评测器、指标方向、候选空间、预算和停止条件必须冻结。
- 代码补丁模式不得修改保护文件，Reject 后必须回滚工作区。
- 配置搜索必须区分 Search 与 Holdout，最终结论不得声称全局最优。
- Docker 不可用时不得在生产配置中静默退化为 Mock Sandbox。

## 6. 开发记录

每个可交付步骤都必须同步创建或更新一份开发记录，统一存放在
`docs/development/`。文档命名为 `YYYY-MM-DD_<英文短名称>.md`，并以
`docs/development/_TEMPLATE.md` 为起点。小修复可以追加到当前里程碑记录；形成独立
行为闭环、公共契约变化或重要技术决策时必须新建记录。

开发记录至少包含：

- 文档状态、所属里程碑和最后验证日期。
- 用户需求、业务背景、要解决的问题、范围与非目标。
- 当前基线及参考项目中借鉴的业务语义，明确哪些已经实现、哪些仍是规划。
- 实现方案、关键流程、代码和测试文件映射，以及主要设计取舍。
- 技术难点、根因、候选方案、最终解决思路和选择理由。
- 实际执行的验证命令、结果和可定位的证据；未运行的检查必须明确说明。
- 当前限制、风险、非承诺边界和下一步建议。
- 变更历史；实验类工作还必须记录环境、数据、参数、命令、结果和产物位置。

文档中的能力声明必须能关联到代码、测试或实验产物，不得用未来设计描述当前能力。
PRD 负责说明“为什么做、做什么”，架构文档负责稳定边界，开发记录负责说明“这一
步如何实现、如何证明”。开发记录是完成标准的一部分，不是事后补写的可选材料。

## 7. 测试与验证

安装开发依赖：

```powershell
python -m pip install -e ".[dev]"
```

常用检查：

```powershell
python -m pytest -q -W error::DeprecationWarning
python -m mypy src/researchflow
python -m ruff check .
python -m ruff format --check .
python -m compileall -q src tests
```

涉及公共行为时至少覆盖：

- 正常路径和依赖顺序。
- 边界输入、重复命令和非法状态迁移。
- 能力、规划器或存储失败的错误语义。
- 暂停/取消与执行完成竞争时的迟到结果隔离。
- 乐观锁冲突、事件顺序和状态/事件原子性。
- API 状态码、响应 schema 和领域错误映射。

不能运行的检查必须在交付说明中明确列出，不得把未执行项目写成已通过。

## 8. 修改流程

1. 修改前查看 `git status`，保留用户已有改动。
2. 先定位模块、调用链、相似实现和现有测试，再决定新增文件或抽象。
3. 使用最小可交付改动完成一个行为闭环。
4. 同步更新测试和对应开发记录；公共契约或用户可见行为变化时同步更新 PRD 或架构文档。
5. 运行与风险匹配的测试、类型、静态和构建检查。
6. 检查 diff 中是否包含生成物、密钥、Token、本地路径或无关文件。

未经用户明确要求，不要提交、推送、创建 PR、改写历史或删除文件。需要发布时，优先创建 `agent/<description>` 分支，使用简洁的中文提交说明，并在 PR 中写明改动、原因、影响和验证结果。

## 9. 完成标准

任务只有在以下条件满足后才算完成：

- 行为符合 PRD 和架构边界。
- 状态、事件、错误和外部协议没有绕层。
- 正常、边界和失败路径具有验证证据。
- 工作区未混入无关文件或敏感信息。
- 文档能准确描述当前实现，而不是未来设想。
- 对应开发记录已包含需求、背景、实现、难点、解决思路和验证证据。
